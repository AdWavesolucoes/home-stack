services:
  metrics-api:
    build: .
    container_name: metrics-api
    restart: unless-stopped
    ports:
      - "5000:5000"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    pid: host
